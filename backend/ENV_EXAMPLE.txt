# === Django Settings ===
# Секретный ключ (смените в продакшене)
SECRET_KEY=django-insecure-your-secret-key-here
# Режим отладки (True/False)
DEBUG=True
# Список разрешенных хостов (через запятую)
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# === Database Settings ===
# Используйте 'postgres' для Docker или 'reaction_game_db' для локального тестирования
DB_NAME=reaction_game_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=db
DB_PORT=5432

# === Docker Compose Specific ===
# Используется в entrypoint.sh для ожидания БД
DATABASE=postgres
