# Reaction Game - Игра на реакцию

Веб-приложение для тренировки реакции с системой достижений, таблицей лидеров и системой друзей. Проект разработан на Django (Backend) и Vue.js (Frontend).

## 🚀 Технологии

**Backend:**
- Django 5.0.1 (REST Framework)
- PostgreSQL (База данных)
- JWT (Аутентификация)
- pytest (Автоматизированное тестирование)
- openpyxl (Экспорт в XLSX)

**Frontend:**
- Vue.js 3 (Composition API)
- Vite (Сборка)
- Axios (API запросы)
- Vanilla CSS (Стильный и адаптивный дизайн)

**DevOps:**
- Docker & Docker Compose
- Nginx (Production-ready serving)

## 📦 Установка и запуск

### 1. Подготовка
- Убедитесь, что у вас установлен **Docker** и **Docker Compose**.
- Клонируйте репозиторий:
  ```bash
  git clone <repository-url>
  cd reaction-game-vanilla
  ```

### 2. Запуск через Docker
```bash
# Сборка и запуск всех контейнеров
docker-compose up --build
```
При первом запуске автоматически применятся миграции БД.

### 3. Инициализация данных
Для работы достижений их необходимо добавить в базу данных:
```bash
docker-compose exec backend python seed_achievements.py
```

Приложение будет доступно по адресам:
- **Frontend**: http://localhost:8080
- **Backend API**: http://localhost:8000/api/
- **Admin Panel**: http://localhost:8000/admin/
- **Swagger**: http://localhost:8000/api/schema/swagger-ui/

### 👤 Учетные данные
По умолчанию в системе может быть создан администратор (если вы запускали соответствующие скрипты):
- **Username**: `admin`
- **Password**: `12345678`

## 🧪 Тестирование

Проект покрыт более чем 50 автоматизированными тестами.

```bash
# Запустить все тесты
docker-compose exec backend pytest

# Подробный вывод с покрытием
docker-compose exec backend pytest -v --cov=games
```

Подробная документация по тестам и ручному тестированию: [TESTING.md](TESTING.md)

## 📊 Основной функционал

- 🎮 **Игра**: 3 уровня сложности, динамические цели, расчет средней реакции.
- 🏆 **Лидерборд**: Глобальный рейтинг игроков с фильтрацией по сложности.
- 🎖️ **Достижения**: Автоматическое начисление достижений ("Новичок", "Снайпер", "Молниеносный").
- 👥 **Система друзей**: Поиск пользователей, отправка запросов, просмотр профилей друзей.
- 📂 **Экспорт**: Администраторы могут выгружать историю игр и таблицу лидеров в формат **XLSX**.

## 📁 Структура проекта

- `backend/` - Django приложение, API и логика БД.
- `frontend/` - Vue.js приложение, интерфейс и игровая логика.
- `docker-compose.yml` - Оркестрация контейнеров.
- `TESTING.md` - Руководство по тестированию.

## 🛠️ Основные эндпоинты API

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| `POST` | `/api/auth/register/` | Регистрация нового пользователя |
| `POST` | `/api/auth/login/` | Получение токенов доступа |
| `GET` | `/api/games/sessions/` | История игр текущего пользователя |
| `GET` | `/api/games/leaderboard/` | Глобальная таблица лидеров |
| `GET` | `/api/games/achievements/` | Список доступных достижений |
| `POST` | `/api/games/friends/` | Отправка запроса в друзья |

## 📝 Соответствие требованиям

Проект полностью соответствует учебным требованиям (Lab 12):
- ✅ Реализованы все модели БД и связи.
- ✅ Полноценная система аутентификации.
- ✅ CRUD операции для игровых сессий и друзей.
- ✅ Автоматизированные тесты моделей, вьюх и безопасности.
- ✅ Контейнеризация и документация.

---
*Проект разработан в рамках курса "Языки и среды разработки Интернет ресурсов".*
