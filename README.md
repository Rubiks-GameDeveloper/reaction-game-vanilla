# Reaction Game - Игра на реакцию

Веб-приложение для тренировки реакции с системой достижений, таблицей лидеров и социальными функциями.

## 🚀 Технологии

**Backend:**
- Django 5.0.1
- Django REST Framework
- PostgreSQL
- JWT Authentication
- pytest для тестирования

**Frontend:**
- Vue.js 3
- Vite
- Axios

**DevOps:**
- Docker & Docker Compose
- Nginx

## 📦 Установка и запуск

### Предварительные требования:
- Docker
- Docker Compose

### Запуск приложения:

```bash
# Клонировать репозиторий
git clone <repository-url>
cd reaction-game-vanilla

# Запустить с помощью Docker Compose
docker-compose up --build
```

Приложение будет доступно по адресам:
- **Frontend**: http://localhost:8080
- **Backend API**: http://localhost:8000/api/
- **Admin Panel**: http://localhost:8000/admin/
- **Swagger**: http://localhost:8000/api/schema/swagger-ui/

### Учетные данные администратора:
- **Username**: admin
- **Password**: 12345678

## 🧪 Тестирование

### Запуск автоматических тестов:

```bash
# Запустить все тесты
docker-compose exec backend pytest

# Запустить с подробным выводом
docker-compose exec backend pytest -v

# Запустить конкретный файл
docker-compose exec backend pytest games/tests/test_models.py

# Запустить с покрытием кода
docker-compose exec backend pytest --cov=games
```

### Типы тестов:

1. **test_models.py** - Тесты моделей
   - Создание объектов
   - Связи между моделями
   - Валидация данных
   - Временные метки

2. **test_views.py** - Тесты API
   - HTTP-ответы
   - Аутентификация
   - CRUD-операции
   - Контроль доступа

3. **test_security.py** - Тесты безопасности
   - Хеширование паролей
   - SQL-инъекции
   - XSS-атаки
   - Экспорт XLSX
   - Интеграционные тесты

Подробная документация по тестированию: [TESTING.md](TESTING.md)

## 📊 Функционал

### Для гостей:
- ✅ Просмотр таблицы лидеров
- ✅ Просмотр списка достижений

### Для авторизованных пользователей:
- ✅ Игра с тремя уровнями сложности
- ✅ Сохранение/загрузка игровых сессий
- ✅ Получение достижений
- ✅ Участие в рейтинге
- ✅ Система друзей (поиск, добавление, просмотр профилей)
- ✅ Просмотр собственного профиля

### Для администраторов:
- ✅ Полный доступ к Django Admin
- ✅ Управление пользователями
- ✅ Просмотр всех игровых сессий
- ✅ Экспорт отчетов в XLSX
- ✅ Назначение достижений вручную

## 🎮 Игровой процесс

1. Выберите уровень сложности (Легкий/Средний/Сложный)
2. Кликайте по появляющимся целям как можно быстрее
3. Набирайте очки и улучшайте свою реакцию
4. Получайте достижения за успехи
5. Соревнуйтесь с друзьями в таблице лидеров

## 📈 Достижения

- **Новичок**: Сыграйте 3 игры
- **Снайпер**: Наберите 500 очков за одну игру
- **Молниеносный**: Достигните средней реакции ≤ 500мс

## 🔒 Безопасность

- ✅ Пароли хранятся в хешированном виде (PBKDF2-SHA256)
- ✅ Защита от SQL-инъекций (Django ORM)
- ✅ Защита от XSS (автоматическое экранирование)
- ✅ JWT токены для аутентификации
- ✅ CORS настроен корректно
- ✅ CSRF защита включена

## 📁 Структура проекта

```
reaction-game-vanilla/
├── backend/                 # Django backend
│   ├── accounts/           # Приложение пользователей
│   ├── games/              # Основное приложение
│   │   ├── models.py       # Модели данных
│   │   ├── views.py        # API views
│   │   ├── serializers.py  # DRF serializers
│   │   ├── admin.py        # Админ-панель
│   │   └── tests/          # Тесты
│   ├── reaction_game/      # Настройки проекта
│   └── requirements.txt    # Python зависимости
├── frontend/               # Vue.js frontend
│   ├── src/
│   │   ├── components/     # Vue компоненты
│   │   ├── composables/    # Composables (useApi, useGame)
│   │   └── App.vue         # Главный компонент
│   └── package.json        # Node зависимости
├── docker-compose.yml      # Docker Compose конфигурация
├── TESTING.md             # Документация по тестированию
└── README.md              # Этот файл
```

## 🛠️ API Endpoints

### Аутентификация:
- `POST /api/accounts/register/` - Регистрация
- `POST /api/accounts/login/` - Вход
- `POST /api/accounts/token/refresh/` - Обновление токена
- `GET /api/accounts/profile/` - Профиль пользователя

### Игровые сессии:
- `GET /api/games/sessions/` - Список сессий
- `POST /api/games/sessions/` - Создать сессию
- `GET /api/games/sessions/latest/` - Последняя сессия

### Таблица лидеров:
- `GET /api/games/leaderboard/` - Таблица лидеров
- `GET /api/games/leaderboard/?difficulty=easy` - Фильтр по сложности

### Достижения:
- `GET /api/games/achievements/` - Все достижения
- `GET /api/games/achievements/user_achievements/` - Достижения пользователя

### Друзья:
- `GET /api/games/friends/search/?q=username` - Поиск пользователей
- `POST /api/games/friends/` - Отправить запрос
- `GET /api/games/friends/friends/` - Список друзей
- `GET /api/games/friends/requests_received/` - Входящие запросы
- `POST /api/games/friends/{id}/accept/` - Принять запрос
- `POST /api/games/friends/{id}/reject/` - Отклонить запрос
- `GET /api/games/friends/{user_id}/profile/` - Профиль друга

## 📝 Соответствие требованиям

Полный отчет о соответствии требованиям: [COMPLIANCE_REPORT.md](COMPLIANCE_REPORT.md)

- ✅ Все модели данных реализованы
- ✅ Три уровня доступа (гость, пользователь, админ)
- ✅ Полный основной функционал
- ✅ Дополнительный функционал (система друзей)
- ✅ Экспорт XLSX отчетов
- ✅ Контейнеризация (Docker)
- ✅ Безопасность (хеширование, защита от инъекций)
- ✅ Комплексное тестирование (50+ тестов)

## 👥 Авторы

Проект разработан в рамках курса "Языки и среды разработки Интернет ресурсов"

## 📄 Лицензия

MIT License
